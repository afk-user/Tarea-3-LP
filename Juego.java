//import java.lang.Math;

class Juego {
    public static void main(String[] args){
        // crear un arreglo de Zonas de tamaño 11 y aleatorizar la forma en la que aparecen las subzonas
        /*Zona zones[] = new Zona[10];
        for(int i = 0;i <= 11;i++){
            int random_num = (int)(Math.random()*3+1);
            switch(random_num){
                case 1:
                    zones[i] = Enemigo.class;
            }
        }*/
    }
}